<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flutterdemo">

    <application
        android:name="io.flutter.app.FlutterApplication"
        android:label="example"
        android:icon="@mipmap/ic_launcher">

        <!-- Agrega la siguiente línea de meta-data para especificar la orientación predeterminada de la pantalla -->
        <meta-data
            android:name="io.flutter.embedding.android.Orientation"
            android:value="portrait" />

        <!-- Agrega la siguiente línea de meta-data para especificar el tema predeterminado de la aplicación Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <!-- Agrega la siguiente línea de intent-filter para definir esta actividad como la actividad de inicio -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
